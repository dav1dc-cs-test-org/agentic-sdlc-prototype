{
  "schemaVersion": 2,
  "issueNumber": 19,
  "requester": "dav1dc-github",
  "request": "[Feature] Turtle Graphics Game for Kids 5-7\n\n### Objective\n\nBuild a small turtle graphics web app for children aged 5-7. Children move a\nvisible turtle around a drawing board, turn it, and lift or lower its pen to\nmake simple artwork. Immediate visual feedback should make direction, distance,\nright-angle turns, and drawing versus movement tangible without requiring\ntyping, reading code, signing in, or adult setup inside the app.\n\nThe first screen is the usable playground, not a landing page. This is creative\nplay, not a scored game, course, or programming-language implementation.\n\n### Scope Baseline\n\nThe requester confirmed the age group and exactly six buttons:\n\n1. Pen Up\n2. Pen Down\n3. Move Forward\n4. Move Backwards\n5. Turn Left 90 degrees\n6. Turn Right 90 degrees\n\nAll six commands are in scope. The board, turtle artwork, pen-state feedback,\nresponsive controls, tests, and local launch documentation complete the feature.\nThere is no command editor, command history, or additional application control.\n\n### Assumptions for Review\n\n- Movement is one fixed-size step per activation; there is no distance input.\n- The pen starts down so the first forward move draws immediately.\n- One fixed ink color is enough for this pilot. The turtle remains visually\n  distinct from the ink.\n- Refreshing the browser starts a new drawing. A dedicated reset button,\n  undo, color picker, and export can be separate follow-up features.\n- The application is served locally or by an existing static host. This issue\n  does not provision public hosting or change GitHub settings.\n\n### Acceptance Criteria\n\n### User Scenarios & Testing\n\n**P1: Draw a first shape.** Given a fresh board, when a child alternates Move\nForward and Turn Right 90 degrees four times, then a square appears, the turtle\nreturns to its starting position facing up, and no extra segments appear.\n\n**P1: Move without leaving a mark.** Given a fresh board, when a child presses\nPen Up, Move Forward, Pen Down, Move Forward, then only the second movement\ndraws a line. The visible pen indicator follows both pen commands.\n\n**P2: Explore and recover at an edge.** Given the turtle has reached the top\nmovement boundary, another forward move leaves it in place. A backward move\nbrings it back into the board. No dialog interrupts play, and the controls\nremain usable.\n\n### Requirements\n\n**REQ-001: Immediate playground.** On load, show one square drawing board,\none recognizable turtle, exactly the six buttons named above, and a short\npen-state indicator. No login, modal, instruction screen, or start button is\nrequired before drawing. No keyboard input beyond optional button activation\nis required.\n\n**REQ-002: Initial state and coordinates.** Use a 400-by-400 logical drawing\nspace with the origin at its top-left, positive x to the right, and positive y\ndownward. Start the turtle at (200, 200), facing up, with its pen down and no\ndrawn segments. The coordinate system is an acceptance-test reference, not\nrequired on-screen text for children.\n\n**REQ-003: Forward movement.** Move Forward moves exactly 20 logical units\nin the direction the turtle faces, subject to the boundary rule below. It\ndoes not change the turtle's heading or pen state.\n\n**REQ-004: Backward movement.** Move Backwards moves exactly 20 logical units\nopposite the direction the turtle faces, subject to the same boundary rule.\nIt does not turn the turtle around or change the pen state.\n\n**REQ-005: Left turn.** Turn Left 90 degrees rotates the turtle one quarter\nturn counterclockwise without changing its position or drawing anything.\nThe heading cycle is up, left, down, right, up.\n\n**REQ-006: Right turn.** Turn Right 90 degrees rotates the turtle one quarter\nturn clockwise without changing its position or drawing anything.\nThe heading cycle is up, right, down, left, up.\n\n**REQ-007: Pen up.** Pen Up changes subsequent movements to travel without\ndrawing. It does not move the turtle, erase artwork, or draw a mark. Repeating\nPen Up has no additional effect.\n\n**REQ-008: Pen down.** Pen Down changes subsequent successful movements to\ndraw. It does not move the turtle or draw a mark by itself. Repeating Pen Down\nhas no additional effect. Both pen buttons remain available, with the active\nmode distinguished visually and programmatically, not by color alone.\n\n**REQ-009: Artwork semantics.** A successful pen-down movement adds exactly\none straight segment between the old and new positions. Use a fixed dark ink\nstroke with rounded ends, 4 logical units wide. Existing segments persist;\nturns, pen changes, and pen-up movements never modify them. The turtle cursor\nis separate from the artwork and does not leave cursor-shaped trails.\n\n**REQ-010: Safe boundaries.** Turtle-center coordinates must stay between\n20 and 380 inclusive on both axes. A movement that would leave this range is\na no-op: no movement, line, wraparound, automatic turn, or pen-state change.\nThe whole turtle remains visible at the boundary, and another valid command\ncan move it away. For example, 20 consecutive forward presses from the initial\nstate end at (200, 20) with exactly nine drawn segments.\n\n**REQ-011: One activation, one command.** Each completed click, tap, Enter,\nor Space activation of a focused button applies exactly one command. Commands\nare processed in activation order. Pointer holding must not start continuous\nmovement, and a tap must not be processed twice through separate touch and\nclick handlers. There is no queued animation or automatic drawing loop.\n\n**REQ-012: Visible cause and effect.** The turtle has a discernible head and\norientation and is always drawn above the artwork. Movement and turns update\nit immediately; pen changes update the visible Pen Up or Pen Down state in\nthe same interaction. Use original, self-contained turtle artwork and familiar\ndirection/pen symbols paired with readable button labels. Do not add tutorial\nparagraphs, scoring, or celebratory interruptions.\n\n**REQ-013: Child-friendly access.** Every button has an accessible name matching\nits action, a visible keyboard-focus indicator, and a touch target of at least\n56 by 56 CSS pixels. Labels may wrap but must not be clipped. All six commands\nwork with pointer, touch, and keyboard. The drawing surface has an accessible\nname, and changes of heading and pen state have a concise nonvisual equivalent.\n\n**REQ-014: Responsive layout without data loss.** At viewport widths of 320,\n390, 768, and 1280 CSS pixels, the complete square board and all controls fit\nwithout horizontal page scrolling, overlap, or clipped labels. Vertical page\nscrolling is acceptable. The board scales visually without changing the\nlogical step size, turtle state, or saved segments. Resizing or changing device\norientation preserves the drawing. Controls remain operable at 200% text zoom.\n\n**REQ-015: Predictable reset and privacy.** Refreshing the page restores the\ninitial state and empty board. Drawings and interactions remain in page memory\nonly: no cookies, browser persistence, accounts, analytics, uploads, or external\nrequests beyond loading the application's own static assets.\n\n**REQ-016: Deterministic, responsive behavior.** Replaying the same finite command\nsequence from the same initial state produces the same heading, pen state,\nposition, and ordered drawing segments. A sequence of 500 alternating forward\nand backward activations remains responsive and produces exactly 500 segments\nwhen the pen stays down; no movement is lost or duplicated. No controls depend\non an inference service or a network connection after the app's assets load.\n\n### Key Entities\n\n- **Turtle:** logical position, one of four headings, and pen-up/pen-down state.\n- **Artwork:** the ordered line segments produced by successful pen-down moves.\n- **Command:** one of the six button actions, with no child-supplied code or\n  arbitrary executable content.\n\n### Success Criteria\n\n- A child can make the first visible line with one button activation from the\n  initial screen; no setup or reading of instructions is necessary.\n- The square scenario produces exactly four sides and returns to (200, 200)\n  facing up. Four left turns also restore the initial heading without drawing.\n- The pen-up scenario ends at (200, 160) with one segment from (200, 180) to\n  (200, 160), leaving the first 20-unit movement blank.\n- At all four specified viewport widths, every control remains available and\n  the turtle and board remain inspectable. Desktop and touch-browser checks\n  show the artwork and cursor, not only an empty drawing element.\n- The page loads and the scenarios complete without uncaught browser errors\n  or third-party network requests. Refreshing produces an empty board.\n\n\n### Constraints and Non-goals\n\n- Fit the implementation into at most six dependency-ordered tasks and the\n  repository's existing file, job, repair, and inference budgets.\n- Do not change the controller, cost accounting, migration code, workflows,\n  credentials, permissions, state branch, or enforcement policy for this feature.\n- Do not modify dependency manifests or lockfiles, the TypeScript configuration,\n  existing baseline tests, or other protected paths. Do not add, download, or\n  vendor a framework, command-language parser, rendering engine, or test library\n  to work around those restrictions. Native browser graphics are sufficient\n  for this six-command scope.\n- No backend, public deployment, external assets, third-party font requests,\n  advertising, user-generated sharing, multiplayer, or collection of children's\n  personal information. Keep the experience self-contained and English-only.\n- No text programming, Logo compatibility, loops, variables, procedures, blocks,\n  command recording, replay UI, drawing challenges, badges, or lesson system.\n- No extra application buttons, menus, adjustable distances or angles, pen-width\n  control, color selection, undo/redo, reset control, save/import, or export.\n- No camera, microphone, location, clipboard, or other permission prompts.\n- Do not weaken coverage or security gates. Both line and branch coverage must\n  meet policy and must not drop from the baseline measured for this new issue.\n  Do not copy an old numerical coverage baseline into the new implementation.\n\n\n### Context\n\nThis is a new feature and a fresh end-to-end exercise of the agentic SDLC\npipeline, not a continuation of issue #1 or its existing PR. The template is\n[the Agentic Feature form](../.github/ISSUE_TEMPLATE/agentic-feature.yml).\nRepository constraints are defined by [the policy](../.github/sdlc/policy.json),\n[the build configuration](../tsconfig.json), and\n[the validation runner](../src/validate.ts).\n\n### Implementation and Verification Boundaries\n\n- Place new TypeScript behavior under `src/turtle-graphics/`, browser assets\n  under `apps/turtle-graphics/`, and new discovered Node tests under `test/`\n  with filenames matching `test/**/*.test.ts`. Update the README and relevant\n  feature documentation to describe the actual local launch procedure.\n- Reuse the existing `npm run build` and Node 24.8+ toolchain. The research plan\n  must specify the browser entry point, build output references, and a runnable\n  local static-serving command without changing protected configuration.\n- Browser modules must not import controller or Node-only modules. Keep turtle\n  transitions and drawing data independently testable, using the same logic in\n  the application and Node tests. Do not duplicate an engine for the browser,\n  omit behavioral code from coverage, or assert only that files exist.\n- Run `npm ci --ignore-scripts` and `npm run verify`. Add regression tests for\n  all headings, forward/backward symmetry, turns, pen idempotence, mixed command\n  sequences, all four boundaries and corners, and drawing/state preservation.\n- The current required pipeline validates Node tests, not browser interactions.\n  Its research plan must acknowledge that limit and describe desktop and touch\n  browser verification of rendering, buttons, resize behavior, accessibility,\n  and console/network errors. Use browser automation if available without\n  changing protected dependencies; otherwise document explicit human checks\n  at PR review. Never report a browser check as passed from Node tests alone.\n\n### Pipeline Pilot Checks\n\nThese are checks on the existing pipeline, not features of the children's app:\n\n1. Create a new issue with this final scope. Have an authorized repository\n   writer apply `agentic-SDLC` after the text is complete. The human label event\n   starts intake; the research plan still requires explicit versioned approval.\n2. Confirm a new lifecycle, plan, branch, job sequence, and cost ledger are used;\n   do not reuse or edit the old issue's state. The new cost history is complete\n   from intake, rather than carrying a legacy-history warning.\n3. Let research, decomposition, implementation, scanning, security, testing,\n   validation, documentation, and final review follow the normal approved flow.\n   Do not force retries or extra inference just to create spending data.\n4. When the final PR is ready, inspect its runner minutes, AI credits, number\n   of recorded runs, and near-limit/pre-emption counts. Actual work should have\n   recorded nonzero duration and inference usage. Compare these with the issue\n   status; do not invent costs or present them as dollar charges.\n5. A later reconciliation with no new worker run must not charge the same work\n   again. The PR must still reference the approved plan and current-commit gate\n   evidence. Review the browser-verification evidence or perform the documented\n   browser checks before deciding whether to merge.",
  "phase": "testing",
  "baseSha": "39ee481bbf98674bf6779fe7a93df57e62255f1c",
  "headSha": "347dcaefd8ec8697c6d8e3d989d8ccadc120568e",
  "controlSha": "39ee481bbf98674bf6779fe7a93df57e62255f1c",
  "baseBranch": "main",
  "branch": "agentic/epic-19-v1",
  "plan": {
    "version": 1,
    "body": "## Summary\n\nBuild a six-button turtle graphics playground for children aged 5-7, entirely\nclient-side, reusing the existing Node/TypeScript toolchain without adding\ndependencies, frameworks, or protected-path changes.\n\n## Approach\n\n**Shared engine (`src/turtle-graphics/engine.ts`, new directory under\n`sourcePaths: src/**/*.ts`, so it is not a protected top-level `src/*.ts`\nfile):**\n- Pure, framework-free TypeScript module with no DOM or Node-only imports, so\n  it runs unmodified in both Node tests and the browser (loaded as an ES\n  module `<script type=\"module\">`).\n- Types: `Heading = 'up' | 'left' | 'down' | 'right'`, `PenState = 'up' |\n  'down'`, `Point = { x: number; y: number }`, `Segment = { from: Point; to:\n  Point }`, `TurtleState = { position: Point; heading: Heading; pen: PenState;\n  segments: Segment[] }`.\n- `createInitialState(): TurtleState` — position (200, 200), heading `'up'`,\n  pen `'down'`, empty segments, matching REQ-002.\n- Pure reducer `applyCommand(state, command): TurtleState` where `command` is\n  one of `'penUp' | 'penDown' | 'forward' | 'backward' | 'turnLeft' |\n  'turnRight'`. Returns a new state object (never mutates), so tests can\n  assert deterministic replay (REQ-016) and the UI can re-render from a single\n  state snapshot.\n- Movement math: fixed step `STEP = 20`; heading vectors `up: (0,-1), down:\n  (0,1), left: (-1,0), right: (1,0)`; candidate position computed, then\n  clamped-range check `20 <= x <= 380` and `20 <= y <= 380` (REQ-010). Out of\n  range -> return the *same* state object reference (no-op, easy to assert\n  with `assert.equal`, not just `deepEqual`). In range with pen down -> append\n  one segment; pen up -> move without appending.\n- Turns rotate the heading via a fixed cycle array and do not touch position\n  or segments (REQ-005/006).\n- Pen commands only change `pen`; repeating a pen command returns the same\n  reference (idempotent per REQ-007/008) to make no-op assertions simple.\n- No timers, no animation queue — one call, one state transition (REQ-011,\n  REQ-016).\n\n**Browser assets (`apps/turtle-graphics/`):**\n- `index.html`: single static page, one square SVG or `<canvas>` board\n  (400x400 logical, scaled responsively via CSS `max-width` / `aspect-ratio`\n  so REQ-014's four breakpoints (320/390/768/1280) all fit without horizontal\n  scroll), one turtle glyph, a pen-state indicator (`aria-live=\"polite\"` text\n  region, not color-only per REQ-008/013), and exactly six `<button>` elements\n  labelled Pen Up / Pen Down / Move Forward / Move Backwards / Turn Left 90° /\n  Turn Right 90°, each ≥56×56 CSS px with visible `:focus-visible` outlines.\n- `app.ts` (compiled alongside `src/**/*.ts` by the existing `tsc` config, or\n  `apps/turtle-graphics/app.ts` included via a small dedicated `tsconfig`\n  reference — see Build/Verification below): imports `applyCommand`/\n  `createInitialState` from `../../src/turtle-graphics/engine.ts`, wires each\n  button to a single `click` handler (keyboard Enter/Space activate native\n  `<button>` semantics for free) and guards against duplicate touch+click\n  firing by relying only on the standard `click` event (no separate\n  `touchstart` handler), satisfying REQ-011. Re-renders the SVG/canvas from\n  the full state on every command (turtle above artwork, per REQ-012) and\n  updates the pen indicator text.\n- No inline event handlers with `eval`-like content, no external requests,\n  no fonts, no analytics (REQ-015).\n\n**Build and local launch (no protected-path or dependency changes):**\n- Reuse `npm run build` (`tsc -p tsconfig.json`) by adding\n  `apps/turtle-graphics/app.ts` under the existing `include` globs only if\n  `tsconfig.json` itself is left untouched — since `tsconfig.json` is a\n  protected path, prefer instead including `apps/**/*.ts` implicitly by\n  keeping it under `src/turtle-graphics/` for logic and placing only a thin\n  non-compiled bootstrap in `apps/`, OR compile the app entry with a\n  one-off `tsc` invocation documented in the README (e.g. `npx tsc\n  apps/turtle-graphics/app.ts --outDir apps/turtle-graphics/dist --module es2022\n  --target es2023 --moduleResolution bundler`) that does not alter the\n  committed `tsconfig.json`. The exact mechanism is an open question for the\n  decomposition task to finalize with a minimal, testable choice (see\n  Unresolved Questions).\n- Local static serving documented in README: `npx --yes serve apps/turtle-graphics`\n  or `python3 -m http.server --directory apps/turtle-graphics 8080` (no new\n  dependency; `serve` runs via npx without being added to `package.json`).\n\n## Task Breakdown (fits in 6 tasks)\n\n1. **Engine**: `src/turtle-graphics/engine.ts` + `test/turtle-graphics/engine.test.ts`\n   covering all headings, forward/backward symmetry, both turn directions,\n   pen idempotence, mixed sequences, all four boundary edges and corners, the\n   500-command replay determinism check (REQ-016), and the documented square\n   and pen-up scenarios from Acceptance Criteria.\n2. **Browser UI**: `apps/turtle-graphics/index.html` + `app.ts` wiring the six\n   buttons, board rendering, pen indicator, and accessibility attributes.\n3. **Responsive styling**: CSS for the four breakpoints, focus rings, 56×56\n   touch targets, 200% zoom behavior.\n4. **Security review task** (existing pipeline stage, not a new task) — no\n   separate implementation task needed; engine has no I/O.\n5. **Testing task**: additional regression tests only, no baseline test\n   modification, under `test/turtle-graphics/`.\n6. **Documentation task**: update `README.md` local-launch section and add\n   `docs/turtle-graphics.md` (or extend `docs/architecture.md`) describing the\n   static serving command and manual browser-verification checklist.\n\nThis uses 3 implementation tasks (engine, UI, styling) plus the existing\ntesting and documentation stages, well within the 6-task/`maxTasks` policy\nlimit.\n\n## Alternatives Considered\n\n- **Canvas vs. SVG rendering**: SVG chosen tentatively for crisp scaling and\n  simpler DOM-based testing hooks (each segment as a `<line>`), but canvas\n  redraw-from-state is equally simple and avoids DOM node growth over 500\n  segments; either is acceptable and left to the coding task, since the engine\n  is renderer-agnostic.\n- **Single combined task vs. split UI/styling**: kept separate for bounded,\n  reviewable diffs under `maxChangeBytes`/`maxFiles`.\n\n## Non-Goals (per issue)\n\nNo distance/angle inputs, no reset/undo/color/export controls, no backend, no\npersistence, no third-party fonts or libraries, no command language or replay\nUI.\n\n## Security Risks\n\n- Client-only static assets with no network calls beyond same-origin assets:\n  minimal attack surface. No user-supplied text is rendered as HTML (no\n  `innerHTML` with dynamic content), avoiding DOM XSS. No external script\n  sources, satisfying REQ-015 and the \"no third-party requests\" constraint.\n- Ensure `app.ts` never uses `eval`, `Function`, or unsanitized `innerHTML`.\n\n## Validation Strategy\n\n- `npm ci --ignore-scripts && npm run verify` covers the engine via Node\n  tests and coverage thresholds (80% lines / 70% branches, no drop), plus\n  `tsc --noEmit` across `src/**/*.ts` and `test/**/*.ts`.\n- The required pipeline's `validate` stage only runs Node tests — it cannot\n  exercise the browser UI. This plan explicitly does not claim browser\n  verification passes from Node tests alone.\n- Manual/human browser verification checklist (documented in\n  `docs/turtle-graphics.md`, to be performed by the requester or a reviewer\n  before merge, since no browser-automation tool is available in this\n  environment): load `apps/turtle-graphics/index.html` via a local static\n  server in a desktop browser and in a touch-capable device or emulator; run\n  the square scenario (4× forward+right) and confirm return to (200,200)\n  facing up; run the pen-up scenario and confirm exactly one visible segment;\n  drive the turtle to each of the four boundaries and confirm no-op behavior\n  with no console/network errors (check DevTools Network tab shows zero\n  requests beyond the page's own assets); resize/rotate at the four specified\n  widths and confirm no horizontal scroll or clipped labels; keyboard-tab\n  through all six buttons and confirm visible focus and correct activation\n  from Enter and Space.\n\n## Unresolved Questions (for requester/approval)\n\n1. **TypeScript compilation path for the browser entry point**: since\n   `tsconfig.json` is a protected path and cannot be edited, should the coding\n   task (a) hand-author `apps/turtle-graphics/app.js` as plain ES-module\n   JavaScript that directly imports the compiled `dist/src/turtle-graphics/\n   engine.js` produced by the existing `npm run build`, or (b) add a second,\n   new `apps/turtle-graphics/tsconfig.json` (not a protected path, since the\n   protected entry is the root `tsconfig.json` and `**/package.json`/\n   `**/*.instructions.md` patterns don't cover a nested tsconfig) with its own\n   narrow `include`? Recommend (a): plain JS in `apps/`, importing the\n   root-built `dist/src/turtle-graphics/engine.js`, keeps the protected root\n   config untouched and needs no new build step beyond documenting `npm run\n   build` before serving.\n2. **Rendering technology** (SVG vs. canvas) — either satisfies all\n   requirements; recommend SVG for simpler manual/automated DOM inspection,\n   final choice deferred to the coding task.\n3. **Exact turtle glyph and pen-indicator wording** — original, self-contained\n   artwork (no third-party icon font) will be authored inline as an SVG\n   `<path>`; exact shape is a coding-task detail, not a scope decision.\n\n## Sources Consulted\n\n- `.sdlc-context.json` (issue #19 request, policy, job).\n- `source/README.md`, `source/docs/architecture.md` (pipeline lifecycle and\n  constraints).\n- `source/.github/sdlc/policy.json` (protected paths, coverage thresholds,\n  task limits).\n- `source/tsconfig.json`, `source/package.json` (build/test/verify commands,\n  Node engine requirement).\n- `source/src/domain.ts`, `source/test/domain.test.ts` (existing pure-function\n  and test style conventions to follow).\n- `source/src/validate.ts` (confirms only Node tests under `test/**/*.test.ts`\n  are executed by the required pipeline; no browser automation is invoked).",
    "hash": "8ff368d6f83c3b18548b7127d79542532a6d306137c753d59f733fda8d24387f"
  },
  "approval": {
    "actor": "dav1dc-github",
    "commentId": 5642435724,
    "planHash": "8ff368d6f83c3b18548b7127d79542532a6d306137c753d59f733fda8d24387f",
    "at": "2026-09-12T01:10:04Z"
  },
  "tasks": [
    {
      "id": "turtle-engine",
      "title": "Implement turtle graphics engine module",
      "description": "Create src/turtle-graphics/engine.ts as a pure, framework-free TypeScript module (no DOM or Node-only imports) exporting: types Heading ('up'|'left'|'down'|'right'), PenState ('up'|'down'), Point ({x:number;y:number}), Segment ({from:Point;to:Point}), TurtleState ({position:Point;heading:Heading;pen:PenState;segments:Segment[]}); createInitialState(): TurtleState returning position (200,200), heading 'up', pen 'down', empty segments; and a pure reducer applyCommand(state, command) where command is one of 'penUp'|'penDown'|'forward'|'backward'|'turnLeft'|'turnRight', always returning a new state object (never mutating the input) except that true no-ops (boundary rejection, repeated pen command) must return the exact same state object reference. Movement uses a fixed STEP=20 with heading vectors up:(0,-1), down:(0,1), left:(-1,0), right:(1,0); compute the candidate position, clamp-check 20<=x<=380 and 20<=y<=380, and no-op (same reference) when out of range. When in range and pen is down, append exactly one segment; when pen is up, move without appending a segment. Turn commands rotate heading via a fixed cycle and must not alter position or segments. Add test/turtle-graphics/engine.test.ts (new file under testPaths pattern, does not modify any existing baseline test) covering: createInitialState's exact initial values; forward/backward symmetry for all four headings; both turn directions from all four headings; pen command idempotence (second identical pen call returns the same object reference via assert.strictEqual/assert.equal, not just deepEqual); a mixed command sequence; all four boundary edges and all four corners producing same-reference no-ops; the documented 4x(forward+turnRight) square scenario returning to (200,200) facing 'up' with 4 segments; the pen-up scenario producing exactly one visible segment when pen is toggled up then down then moved; and a 500-command deterministic replay check (same command sequence applied twice from createInitialState() produces deep-equal final states, per REQ-016).",
      "acceptance": [
        "src/turtle-graphics/engine.ts exports createInitialState and applyCommand with no DOM or Node-only imports",
        "createInitialState() returns position (200,200), heading 'up', pen 'down', and an empty segments array",
        "applyCommand never mutates its input state and returns a new object for any state-changing command",
        "Boundary-rejected forward/backward moves return the identical input state object reference (no-op)",
        "Repeating an identical pen command (penUp after penUp, or penDown after penDown) returns the identical input state object reference",
        "turnLeft/turnRight only change heading and never change position or segments",
        "forward with pen down appends exactly one segment from the prior position to the new position; pen up moves without appending a segment",
        "test/turtle-graphics/engine.test.ts is a new file that does not modify any existing test under test/**/*.test.ts",
        "Tests cover all four headings, both turn directions, all four boundary edges and corners, pen idempotence, the 4-forward-4-right square scenario returning to (200,200) facing up, the single-segment pen-up scenario, and a 500-command deterministic replay equality check",
        "npm run verify (tsc --noEmit plus Node test run with coverage) passes including the new engine and its tests, meeting the 80% line / 70% branch coverage thresholds with no drop"
      ],
      "dependsOn": [],
      "issueNumber": 20,
      "completed": true
    },
    {
      "id": "turtle-ui",
      "title": "Build turtle graphics browser UI",
      "description": "Create apps/turtle-graphics/index.html and apps/turtle-graphics/app.ts (or hand-authored app.js per the plan's recommended option, importing the tsc-built dist/src/turtle-graphics/engine.js so the protected root tsconfig.json is left untouched) implementing a single static page with one square SVG or <canvas> board (400x400 logical size, responsive via CSS max-width/aspect-ratio), one original inline-SVG turtle glyph (no third-party icon font), an aria-live='polite' pen-state text indicator (not color-only), and exactly six <button> elements labelled Pen Up / Pen Down / Move Forward / Move Backwards / Turn Left 90 degrees / Turn Right 90 degrees, each at least 56x56 CSS px with a visible :focus-visible outline. Each button wires a single 'click' handler only (no separate touchstart handler, avoiding duplicate touch+click firing) that calls applyCommand/createInitialState from ../../src/turtle-graphics/engine.ts (or its built output) and re-renders the full board (turtle glyph drawn above artwork) plus the pen indicator text from the returned state on every command. Do not use eval, Function, or innerHTML with dynamic/unsanitized content, and do not add any external script, font, or analytics requests. Do not modify tsconfig.json, package.json, package-lock.json, or any other protected path; do not add new npm dependencies.",
      "acceptance": [
        "apps/turtle-graphics/index.html renders one board (SVG or canvas), one turtle glyph, an aria-live='polite' pen-state indicator, and exactly six labelled buttons: Pen Up, Pen Down, Move Forward, Move Backwards, Turn Left 90 degrees, Turn Right 90 degrees",
        "Each of the six buttons is a native <button> element at least 56x56 CSS px and has a visible :focus-visible outline style",
        "Each button has exactly one 'click' event handler (no touchstart handler) that calls the corresponding engine command via applyCommand",
        "The board and turtle glyph re-render from the full returned TurtleState after every command, with the turtle glyph drawn above any existing artwork/segments",
        "The pen-state indicator text updates on every pen command and is not conveyed by color alone",
        "No inline eval/Function usage and no innerHTML assignment of dynamic/unsanitized content anywhere in the new UI code",
        "No external script, font, analytics, or network request is added; only same-origin static assets are referenced",
        "tsconfig.json, package.json, package-lock.json, and all other protectedPaths entries are unchanged",
        "npm run build (or the documented one-off tsc invocation for the app entry) succeeds without editing the committed tsconfig.json"
      ],
      "dependsOn": [
        "turtle-engine"
      ],
      "issueNumber": 21,
      "completed": true
    },
    {
      "id": "turtle-styling",
      "title": "Add responsive styling and accessibility polish",
      "description": "Add CSS (inline in apps/turtle-graphics/index.html or as a same-directory apps/turtle-graphics/styles.css referenced only via a relative same-origin link) that makes the board and six-button layout usable without horizontal scrolling or clipped labels at viewport widths 320px, 390px, 768px, and 1280px, keeps all six buttons at least 56x56 CSS px with visible focus rings at every breakpoint, and remains usable/legible at 200% browser zoom. Do not introduce any new npm dependency, external font, or icon library; do not modify tsconfig.json, package.json, package-lock.json, or any other protected path.",
      "acceptance": [
        "At viewport widths 320px, 390px, 768px, and 1280px the page shows no horizontal scrollbar and no clipped button labels",
        "All six buttons remain at least 56x56 CSS px and keep a visible focus outline at each of the four breakpoints",
        "The page remains usable and legible (no overlapping or clipped controls) at 200% browser zoom",
        "No new npm dependency, external font, or icon library is introduced; all styling is same-origin static CSS",
        "tsconfig.json, package.json, package-lock.json, and all other protectedPaths entries are unchanged"
      ],
      "dependsOn": [
        "turtle-ui"
      ],
      "issueNumber": 22,
      "completed": true
    }
  ],
  "tasksLinked": true,
  "retiredTasks": [],
  "evidence": [
    {
      "stage": "scan",
      "sha": "347dcaefd8ec8697c6d8e3d989d8ccadc120568e",
      "jobId": "19-6",
      "runId": 34665689536,
      "summary": "CodeQL, dependency audit, and secret scanning passed."
    },
    {
      "stage": "security",
      "sha": "347dcaefd8ec8697c6d8e3d989d8ccadc120568e",
      "jobId": "19-7",
      "runId": 34665778219,
      "summary": "Reviewed the full diff from baseSha 39ee481bbf98674bf6779fe7a93df57e62255f1c to headSha 347dcaefd8ec8697c6d8e3d989d8ccadc120568e (job 19-7, security stage). Changed files: apps/turtle-graphics/app.js (new, 81 lines), apps/turtle-graphics/index.html (new, 140 lines), src/turtle-graphics/engine.ts (new, 92 lines), test/turtle-graphics/engine.test.ts (new, 172 lines). No changes to package.json, package-lock.json, tsconfig.json, or any .github workflow/action files, and no changes to any protected top-level src/*.ts files.\n\nAuthentication/authorization: not applicable — this is a static, client-side-only feature with no server, no session state, no auth surface.\n\nInjection: engine.ts is a pure, side-effect-free reducer (applyCommand/createInitialState) operating only on an internal typed state object; it accepts no external input. app.js reads a fixed, developer-authored `data-command` attribute from six static <button> elements in index.html and passes it straight to applyCommand's exhaustive Command union switch — no eval(), no Function(), no document.write, no string-built HTML. DOM writes use safe primitives only: `textContent` for the segments container and pen indicator, `setAttribute` with `String(number)` for SVG line/transform coordinates, and `createElementNS` for new SVG nodes. No `innerHTML`/`outerHTML`/`insertAdjacentHTML` usage found (grepped both new files). No user-supplied text is ever rendered, so there is no DOM XSS vector.\n\nUnsafe deserialization: none — no JSON.parse/eval of external data, no localStorage/sessionStorage/cookie use, no network fetch calls (grepped for http://, https://, fetch, XMLHttpRequest; only match is the SVG namespace URI string, which is inert).\n\nSecrets/data exposure: no credentials, tokens, or environment variables referenced; no telemetry or external endpoints added.\n\nDependencies: package.json/package-lock.json untouched; `npm ci --ignore-scripts` shows 0 vulnerabilities and no new packages. app.js imports only from the project's own compiled dist output (../../dist/src/turtle-graphics/engine.js).\n\nWorkflow trust boundaries: no .github/workflows or control/ files touched; tsconfig.json (protected) is unmodified and apps/**/*.ts is deliberately excluded from its build per the file's own header comment, consistent with the approved plan's protected-path constraints.\n\nPrior scanner evidence (job 19-6, run 34665689536) reports CodeQL, dependency audit, and secret scanning passed for this same commit (347dcae); this independent review corroborates that result rather than relying on it alone.\n\nTests: ran `node --experimental-strip-types --test test/turtle-graphics/engine.test.ts` — all 13 tests pass, covering boundary clamping, idempotent pen toggles, non-mutation, and deterministic 500-command replay.\n\nLimitations: review was static/manual (no live browser execution, no dynamic fuzzing, no automated SAST tool run beyond the existing CodeQL/audit/secret-scan evidence cited above). Scope is limited to the diff between the stated base and head commits; this is not a claim that no vulnerability exists anywhere in the repository, only that no actionable security defect was found in the reviewed change."
    }
  ],
  "processedEvents": [
    "comment:5642435724"
  ],
  "sequence": 7,
  "repairs": 0,
  "failures": 0,
  "spend": {
    "runs": 7,
    "runnerMs": 2492000,
    "credits": 327.50968,
    "nearLimit": 0,
    "preempted": 0,
    "historyComplete": true
  },
  "feedback": ""
}
